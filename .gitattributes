# Prevent OneDrive from syncing build directories
.next export-ignore
node_modules export-ignore

# Force all shell scripts to use LF line endings (Critical for Docker on Windows)
*.sh text eol=lf
*.bash text eol=lf
